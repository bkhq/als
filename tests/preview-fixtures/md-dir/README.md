# als preview md-dir fixture

No `book.toml` here. `als preview` detects markdown files at the
root, synthesises a `SUMMARY.md` (root-level files first, then one
part per sub-directory), and serves the result through mdbook.

See `getting-started.md` for a second root-level page and
`guide/` for the part-grouped chapters.
