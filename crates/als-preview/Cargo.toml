[package]
name = "als-preview"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "als CLI local-preview HTTP server: serve a directory as the als-deploy server would"

[lib]
name = "als_preview"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
als-core = { path = "../als-core" }
tiny_http.workspace = true

[dev-dependencies]
tempfile.workspace = true
